#	Example:
# 
#	example_trigger = {
#		tag = GER
#		is_ai = no
#	}
#
#
#	In a script file:
#
#	trigger = {
#		exampel_trigger = yes
#	}
#
can_lose_unity = {
	has_stability > 0.6
	OR = {
		has_war = no
		AND = {
			has_war = yes
			surrender_progress < 0.1
		}
	}
}

can_lose_democracy_support = {
	far_left < 0.18
	left < 0.18
	far_right < 0.18
	center_left > 0.20
	center > 0.20
	center_right > 0.20
}