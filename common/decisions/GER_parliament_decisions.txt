GER_parliament = {

	GER_parliament_election_mission = {
		priority = 1000
		allowed = { always = no }
		icon = generic_political_actions
		visible = { }
		available = { hidden_trigger = { always = no } }
		is_good = yes
		fire_only_once = yes
		selectable_mission = no
		days_mission_timeout = 256
		complete_effect = {
		}
		timeout_effect = {
			custom_effect_tooltip = GER_parliament_election_timeout_tt
			hidden_effect = {
				if = {
					limit = {
						NOT = { has_country_flag = GER_parliament_initialized }
					}
					GER_parliament_initialize = yes
				}
			}
			GER_parliament_hold_election = yes
			hidden_effect = {
				activate_mission = GER_parliament_election_cycle_mission
			}
		}
		ai_will_do = { factor = 0 }
	}

	GER_parliament_election_cycle_mission = {
		priority = 1000
		allowed = { always = no }
		icon = generic_political_actions
		visible = { }
		available = { hidden_trigger = { always = no } }
		is_good = yes
		fire_only_once = yes
		selectable_mission = no
		days_mission_timeout = 1826
		complete_effect = {
		}
		timeout_effect = {
			custom_effect_tooltip = GER_parliament_election_timeout_tt
			hidden_effect = {
				if = {
					limit = {
						NOT = { has_country_flag = GER_parliament_initialized }
					}
					GER_parliament_initialize = yes
				}
			}
			GER_parliament_hold_election = yes
			hidden_effect = {
				activate_mission = GER_parliament_election_cycle_mission
			}
		}
		ai_will_do = { factor = 0 }
	}

	GER_parliament_show_lobbying = {
		priority = 200
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		fire_only_once = no
		allowed = { original_tag = GER }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			NOT = { has_country_flag = GER_parliament_show_lobbying }
		}
		complete_effect = {
			set_country_flag = GER_parliament_show_lobbying
		}
		ai_will_do = { factor = 0 }
	}

	GER_parliament_hide_lobbying = {
		priority = 199
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		fire_only_once = no
		allowed = { original_tag = GER }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_lobbying
		}
		complete_effect = {
			clr_country_flag = GER_parliament_show_lobbying
		}
		ai_will_do = { factor = 0 }
	}

	GER_parliament_show_counter = {
		priority = 180
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		fire_only_once = no
		allowed = { original_tag = GER }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			NOT = { has_country_flag = GER_parliament_show_counter }
		}
		complete_effect = {
			set_country_flag = GER_parliament_show_counter
		}
		ai_will_do = { factor = 0 }
	}

	GER_parliament_hide_counter = {
		priority = 179
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		fire_only_once = no
		allowed = { original_tag = GER }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_counter
		}
		complete_effect = {
			clr_country_flag = GER_parliament_show_counter
		}
		ai_will_do = { factor = 0 }
	}

	GER_parliament_show_operations = {
		priority = 160
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		fire_only_once = no
		allowed = { original_tag = GER }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			NOT = { has_country_flag = GER_parliament_show_operations }
		}
		complete_effect = {
			set_country_flag = GER_parliament_show_operations
		}
		ai_will_do = { factor = 0 }
	}

	GER_parliament_hide_operations = {
		priority = 159
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		fire_only_once = no
		allowed = { original_tag = GER }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_operations
		}
		complete_effect = {
			clr_country_flag = GER_parliament_show_operations
		}
		ai_will_do = { factor = 0 }
	}

	GER_parliament_show_debug = {
		priority = 140
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		fire_only_once = no
		allowed = { original_tag = GER }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			NOT = { has_country_flag = GER_parliament_show_debug }
		}
		complete_effect = {
			set_country_flag = GER_parliament_show_debug
		}
		ai_will_do = { factor = 0 }
	}

	GER_parliament_hide_debug = {
		priority = 139
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		fire_only_once = no
		allowed = { original_tag = GER }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			clr_country_flag = GER_parliament_show_debug
		}
		ai_will_do = { factor = 0 }
	}

	# Lobbying decisions
	GER_lobby_kpd = {
		icon = generic_political_actions
		cost = 50
		days_remove = 30
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_lobbying
		}
		remove_effect = {
			custom_effect_tooltip = GER_lobby_kpd_tt
			add_to_variable = { GER_parliament_kpd_support_bps = 20 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_lobby_uspd = {
		icon = generic_political_actions
		cost = 50
		days_remove = 30
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_lobbying
		}
		remove_effect = {
			custom_effect_tooltip = GER_lobby_uspd_tt
			add_to_variable = { GER_parliament_uspd_support_bps = 20 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_lobby_spd = {
		icon = generic_political_actions
		cost = 50
		days_remove = 30
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_lobbying
		}
		remove_effect = {
			custom_effect_tooltip = GER_lobby_spd_tt
			add_to_variable = { GER_parliament_spd_support_bps = 20 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_lobby_fvp = {
		icon = generic_political_actions
		cost = 50
		days_remove = 30
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_lobbying
		}
		remove_effect = {
			custom_effect_tooltip = GER_lobby_fvp_tt
			add_to_variable = { GER_parliament_fvp_support_bps = 20 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_lobby_zentrum = {
		icon = generic_political_actions
		cost = 50
		days_remove = 30
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_lobbying
		}
		remove_effect = {
			custom_effect_tooltip = GER_lobby_zentrum_tt
			add_to_variable = { GER_parliament_zentrum_support_bps = 20 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_lobby_nlp = {
		icon = generic_political_actions
		cost = 50
		days_remove = 30
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_lobbying
		}
		remove_effect = {
			custom_effect_tooltip = GER_lobby_nlp_tt
			add_to_variable = { GER_parliament_nlp_support_bps = 20 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_lobby_fkp = {
		icon = generic_political_actions
		cost = 50
		days_remove = 30
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_lobbying
		}
		remove_effect = {
			custom_effect_tooltip = GER_lobby_fkp_tt
			add_to_variable = { GER_parliament_fkp_support_bps = 20 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_lobby_dkp = {
		icon = generic_political_actions
		cost = 50
		days_remove = 30
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_lobbying
		}
		remove_effect = {
			custom_effect_tooltip = GER_lobby_dkp_tt
			add_to_variable = { GER_parliament_dkp_support_bps = 20 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_lobby_dvlp = {
		icon = generic_political_actions
		cost = 50
		days_remove = 30
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_lobbying
		}
		remove_effect = {
			custom_effect_tooltip = GER_lobby_dvlp_tt
			add_to_variable = { GER_parliament_dvlp_support_bps = 20 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	# Counter-demonstration decisions (opposition parties only)
	GER_counter_kpd = {
		icon = generic_political_actions
		cost = 25
		days_remove = 30
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_counter
		}
		remove_effect = {
			custom_effect_tooltip = GER_counter_kpd_tt
			add_to_variable = { GER_parliament_kpd_support_bps = -20 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_counter_uspd = {
		icon = generic_political_actions
		cost = 25
		days_remove = 30
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_counter
		}
		remove_effect = {
			custom_effect_tooltip = GER_counter_uspd_tt
			add_to_variable = { GER_parliament_uspd_support_bps = -20 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_counter_spd = {
		icon = generic_political_actions
		cost = 25
		days_remove = 30
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_counter
		}
		remove_effect = {
			custom_effect_tooltip = GER_counter_spd_tt
			add_to_variable = { GER_parliament_spd_support_bps = -20 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_counter_fvp = {
		icon = generic_political_actions
		cost = 25
		days_remove = 30
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_counter
		}
		remove_effect = {
			custom_effect_tooltip = GER_counter_fvp_tt
			add_to_variable = { GER_parliament_fvp_support_bps = -20 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_counter_zentrum = {
		icon = generic_political_actions
		cost = 25
		days_remove = 30
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_counter
		}
		remove_effect = {
			custom_effect_tooltip = GER_counter_zentrum_tt
			add_to_variable = { GER_parliament_zentrum_support_bps = -20 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_counter_dvlp = {
		icon = generic_political_actions
		cost = 25
		days_remove = 30
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_counter
		}
		remove_effect = {
			custom_effect_tooltip = GER_counter_dvlp_tt
			add_to_variable = { GER_parliament_dvlp_support_bps = -20 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	# Influence operations
	GER_operation_kpd = {
		icon = generic_political_actions
		cost = 75
		days_remove = 60
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
			check_variable = { GER_parliament_kpd_seats > 2 }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_operations
			check_variable = { GER_parliament_kpd_seats > 2 }
		}
		remove_effect = {
			custom_effect_tooltip = GER_operation_kpd_tt
			random_list = {
				60 = {
					add_to_variable = { GER_parliament_kpd_seats = -2 }
					add_to_variable = { GER_parliament_dkp_seats = 2 }
					GER_parliament_recalc_government_seats = yes
					GER_parliament_refresh_thresholds = yes
				}
				40 = {
					add_political_power = -10
				}
			}
		}
		ai_will_do = { factor = 0 }
	}

	GER_operation_uspd = {
		icon = generic_political_actions
		cost = 75
		days_remove = 60
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
			check_variable = { GER_parliament_uspd_seats > 2 }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_operations
			check_variable = { GER_parliament_uspd_seats > 2 }
		}
		remove_effect = {
			custom_effect_tooltip = GER_operation_uspd_tt
			random_list = {
				60 = {
					add_to_variable = { GER_parliament_uspd_seats = -2 }
					add_to_variable = { GER_parliament_dkp_seats = 2 }
					GER_parliament_recalc_government_seats = yes
					GER_parliament_refresh_thresholds = yes
				}
				40 = {
					add_political_power = -10
				}
			}
		}
		ai_will_do = { factor = 0 }
	}

	GER_operation_spd = {
		icon = generic_political_actions
		cost = 75
		days_remove = 60
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
			check_variable = { GER_parliament_spd_seats > 2 }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_operations
			check_variable = { GER_parliament_spd_seats > 2 }
		}
		remove_effect = {
			custom_effect_tooltip = GER_operation_spd_tt
			random_list = {
				60 = {
					add_to_variable = { GER_parliament_spd_seats = -2 }
					add_to_variable = { GER_parliament_dkp_seats = 2 }
					GER_parliament_recalc_government_seats = yes
					GER_parliament_refresh_thresholds = yes
				}
				40 = {
					add_political_power = -10
				}
			}
		}
		ai_will_do = { factor = 0 }
	}

	GER_operation_fvp = {
		icon = generic_political_actions
		cost = 75
		days_remove = 60
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
			check_variable = { GER_parliament_fvp_seats > 2 }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_operations
			check_variable = { GER_parliament_fvp_seats > 2 }
		}
		remove_effect = {
			custom_effect_tooltip = GER_operation_fvp_tt
			random_list = {
				60 = {
					add_to_variable = { GER_parliament_fvp_seats = -2 }
					add_to_variable = { GER_parliament_dkp_seats = 2 }
					GER_parliament_recalc_government_seats = yes
					GER_parliament_refresh_thresholds = yes
				}
				40 = {
					add_political_power = -10
				}
			}
		}
		ai_will_do = { factor = 0 }
	}

	GER_operation_zentrum = {
		icon = generic_political_actions
		cost = 75
		days_remove = 60
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
			check_variable = { GER_parliament_zentrum_seats > 2 }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_operations
			check_variable = { GER_parliament_zentrum_seats > 2 }
		}
		remove_effect = {
			custom_effect_tooltip = GER_operation_zentrum_tt
			random_list = {
				60 = {
					add_to_variable = { GER_parliament_zentrum_seats = -2 }
					add_to_variable = { GER_parliament_dkp_seats = 2 }
					GER_parliament_recalc_government_seats = yes
					GER_parliament_refresh_thresholds = yes
				}
				40 = {
					add_political_power = -10
				}
			}
		}
		ai_will_do = { factor = 0 }
	}

	GER_operation_nlp = {
		icon = generic_political_actions
		cost = 75
		days_remove = 60
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_operations
		}
		remove_effect = {
			custom_effect_tooltip = GER_operation_nlp_tt
			random_list = {
				60 = {
					add_to_variable = { GER_parliament_nlp_seats = 2 }
					GER_parliament_recalc_government_seats = yes
					GER_parliament_refresh_thresholds = yes
				}
				40 = {
					add_political_power = -10
				}
			}
		}
		ai_will_do = { factor = 0 }
	}

	GER_operation_fkp = {
		icon = generic_political_actions
		cost = 75
		days_remove = 60
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_operations
		}
		remove_effect = {
			custom_effect_tooltip = GER_operation_fkp_tt
			random_list = {
				60 = {
					add_to_variable = { GER_parliament_fkp_seats = 2 }
					GER_parliament_recalc_government_seats = yes
					GER_parliament_refresh_thresholds = yes
				}
				40 = {
					add_political_power = -10
				}
			}
		}
		ai_will_do = { factor = 0 }
	}

	GER_operation_dkp = {
		icon = generic_political_actions
		cost = 75
		days_remove = 60
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_operations
		}
		remove_effect = {
			custom_effect_tooltip = GER_operation_dkp_tt
			random_list = {
				60 = {
					add_to_variable = { GER_parliament_dkp_seats = 2 }
					GER_parliament_recalc_government_seats = yes
					GER_parliament_refresh_thresholds = yes
				}
				40 = {
					add_political_power = -10
				}
			}
		}
		ai_will_do = { factor = 0 }
	}

	GER_operation_dvlp = {
		icon = generic_political_actions
		cost = 75
		days_remove = 60
		available = {
			has_country_flag = GER_parliament_initialized
			NOT = { has_decision = GER_lobby_kpd }
			NOT = { has_decision = GER_lobby_uspd }
			NOT = { has_decision = GER_lobby_spd }
			NOT = { has_decision = GER_lobby_fvp }
			NOT = { has_decision = GER_lobby_zentrum }
			NOT = { has_decision = GER_lobby_nlp }
			NOT = { has_decision = GER_lobby_fkp }
			NOT = { has_decision = GER_lobby_dkp }
			NOT = { has_decision = GER_lobby_dvlp }
			NOT = { has_decision = GER_counter_kpd }
			NOT = { has_decision = GER_counter_uspd }
			NOT = { has_decision = GER_counter_spd }
			NOT = { has_decision = GER_counter_fvp }
			NOT = { has_decision = GER_counter_zentrum }
			NOT = { has_decision = GER_counter_dvlp }
			NOT = { has_decision = GER_operation_kpd }
			NOT = { has_decision = GER_operation_uspd }
			NOT = { has_decision = GER_operation_spd }
			NOT = { has_decision = GER_operation_fvp }
			NOT = { has_decision = GER_operation_zentrum }
			NOT = { has_decision = GER_operation_nlp }
			NOT = { has_decision = GER_operation_fkp }
			NOT = { has_decision = GER_operation_dkp }
			NOT = { has_decision = GER_operation_dvlp }
			check_variable = { GER_parliament_dvlp_seats > 2 }
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_operations
			check_variable = { GER_parliament_dvlp_seats > 2 }
		}
		remove_effect = {
			custom_effect_tooltip = GER_operation_dvlp_tt
			random_list = {
				60 = {
					add_to_variable = { GER_parliament_dvlp_seats = -2 }
					add_to_variable = { GER_parliament_dkp_seats = 2 }
					GER_parliament_recalc_government_seats = yes
					GER_parliament_refresh_thresholds = yes
				}
				40 = {
					add_political_power = -10
				}
			}
		}
		ai_will_do = { factor = 0 }
	}

	# Debug
	GER_debug_support_kpd_up = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_kpd_up_tt
			add_to_variable = { GER_parliament_kpd_support_bps = 10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_kpd_down = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_kpd_down_tt
			add_to_variable = { GER_parliament_kpd_support_bps = -10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_uspd_up = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_uspd_up_tt
			add_to_variable = { GER_parliament_uspd_support_bps = 10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_uspd_down = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_uspd_down_tt
			add_to_variable = { GER_parliament_uspd_support_bps = -10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_spd_up = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_spd_up_tt
			add_to_variable = { GER_parliament_spd_support_bps = 10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_spd_down = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_spd_down_tt
			add_to_variable = { GER_parliament_spd_support_bps = -10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_fvp_up = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_fvp_up_tt
			add_to_variable = { GER_parliament_fvp_support_bps = 10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_fvp_down = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_fvp_down_tt
			add_to_variable = { GER_parliament_fvp_support_bps = -10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_zentrum_up = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_zentrum_up_tt
			add_to_variable = { GER_parliament_zentrum_support_bps = 10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_zentrum_down = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_zentrum_down_tt
			add_to_variable = { GER_parliament_zentrum_support_bps = -10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_nlp_up = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_nlp_up_tt
			add_to_variable = { GER_parliament_nlp_support_bps = 10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_nlp_down = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_nlp_down_tt
			add_to_variable = { GER_parliament_nlp_support_bps = -10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_fkp_up = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_fkp_up_tt
			add_to_variable = { GER_parliament_fkp_support_bps = 10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_fkp_down = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_fkp_down_tt
			add_to_variable = { GER_parliament_fkp_support_bps = -10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_dkp_up = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_dkp_up_tt
			add_to_variable = { GER_parliament_dkp_support_bps = 10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_dkp_down = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_dkp_down_tt
			add_to_variable = { GER_parliament_dkp_support_bps = -10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_dvlp_up = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_dvlp_up_tt
			add_to_variable = { GER_parliament_dvlp_support_bps = 10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_dvlp_down = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_dvlp_down_tt
			add_to_variable = { GER_parliament_dvlp_support_bps = -10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_misc_up = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_misc_up_tt
			add_to_variable = { GER_parliament_misc_support_bps = 10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_support_misc_down = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = { has_country_flag = GER_parliament_initialized }
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_support_misc_down_tt
			add_to_variable = { GER_parliament_misc_support_bps = -10 }
			GER_parliament_normalize_support = yes
			GER_parliament_refresh_thresholds = yes
		}
		ai_will_do = { factor = 0 }
	}

	GER_debug_election = {
		icon = generic_political_actions
		cost = 0
		days_remove = 0
		available = {
			has_country_flag = GER_parliament_initialized
		}
		visible = {
			original_tag = GER
			has_country_flag = GER_parliament_initialized
			has_country_flag = GER_parliament_show_debug
		}
		complete_effect = {
			custom_effect_tooltip = GER_debug_election_tt
			GER_parliament_hold_election = yes
		}
		ai_will_do = { factor = 0 }
	}
}
