autonomy_state = {
	id = autonomy_dominion
	
	min_freedom_level = 0.8
	
	manpower_influence = 0.2
	
	rule = {
		desc = "RULE_DESC_IS_A_SUBJECT"
	
		can_not_declare_war = yes
		can_decline_call_to_war = no
		can_be_spymaster = yes
	}
	
	modifier = {
		autonomy_manpower_share = 0.0
		extra_trade_to_overlord_factor = 0.25
		overlord_trade_cost_factor = -0.25
		peace_score_ratio_transferred_to_overlord = 0.05
		lend_lease_tension_with_overlord = -0.1
		faction_influence_contribution_factor = -0.1
	}
	
	ai_subject_wants_higher = {
		factor = 0.0
		modifier = {
			has_dlc = "Graveyard of Empires"
			original_tag = RAJ
			always = no # Tsareich2: disabled missing focus reference RAJ_red_in_the_shadows
			add = 10
		}
	}
	
	ai_overlord_wants_lower = {
		factor = 1.0
	}

	ai_overlord_wants_garrison = {
		always = no
	}

	allowed = {
		has_dlc = "Together for Victory"

		if = {
			limit = { has_dlc = "Death or Dishonor" }
			OVERLORD = { NOT = { has_government = far_right } }
		}

		if = {
			limit = {
				has_dlc = "Waking the Tiger"
			}

			NOT = {
				AND = {
					OVERLORD = { has_government = far_right } 
					OVERLORD = { tag = JAP } 
				}
				AND = {
					OVERLORD = { has_government = right } 
					OVERLORD = { tag = JAP } 
				}
			}
		}

		if = {
			limit = {
				has_dlc = "No Compromise, No Surrender"
			}

			NOT = {
				AND = {
					OVERLORD = { has_government = far_right } 
					OVERLORD = { tag = CHI } 
				}
				AND = {
					OVERLORD = { has_government = right } 
					OVERLORD = { tag = CHI } 
				}
			}
		}
	}
	
	can_take_level = {
		if = {
			limit = {
				original_tag = AST
			}
			OR = {
				always = no # Tsareich2: disabled missing focus reference AST_adopt_westminster
				always = no # Tsareich2: disabled missing focus reference AST_abandon_the_westminster_system
				always = no # Tsareich2: disabled missing focus reference AST_sever_ties_with_uk
			}
		}
		if = {
			limit = {
				original_tag = SAF
			}
			always = no # Tsareich2: disabled missing focus reference SAF_abandon_westminster
		}
		IF = {
			limit = {
				original_tag = COG
				has_dlc = "Gotterdammerung"			
			}
			always = no # Tsareich2: disabled missing focus reference CONGO_republic_of_congo
			NOT = {
				always = no # Tsareich2: disabled missing focus reference CONGO_dominion_of_congo
			}
		}
		if = {
			limit = {
				original_tag = RAJ
				has_dlc = "Graveyard of Empires"
				always = yes #NOT = { has_completed_focus = RAJ_his_majestys_loyal_government }
			}
			OR = {
				always = no # Tsareich2: disabled missing focus reference RAJ_a_secular_state
				always = no # Tsareich2: disabled missing focus reference RAJ_united_bengal
				always = no # Tsareich2: disabled missing focus reference RAJ_eastern_pakistan
			}
		}
		if = {
			limit = {
				original_tag = RAJ
				has_dlc = "Graveyard of Empires"
				always = no # Tsareich2: disabled missing focus reference RAJ_his_majestys_loyal_government
			}
			always = no # Tsareich2: disabled missing focus reference RAJ_the_dominion_of_india
			always = no # Tsareich2: disabled missing focus reference RAJ_keep_calm_and_carry_on
			always = no # Tsareich2: disabled missing focus reference RAJ_holding_the_gates_of_india
		}
	}

	can_lose_level = {
		IF = {
			limit = {
				OVERLORD = { original_tag = BEL } 
				original_tag = COG
				has_dlc = "Gotterdammerung"			
			}
			NOT = {
				always = no # Tsareich2: disabled missing focus reference CONGO_dominion_of_congo
			}
		}
	}
}
