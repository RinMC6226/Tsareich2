add_namespace = tsr_jap_treaty

country_event = {
	id = tsr_jap_treaty.1
	title = tsr_jap_treaty.1.t
	desc = tsr_jap_treaty.1.d
	is_triggered_only = yes

	option = {
		name = tsr_jap_treaty.1.a
		add_political_power = -75
		add_war_support = -0.08
	}

	option = {
		name = tsr_jap_treaty.1.b
		if = {
			limit = {
				NOT = { has_war_with = JAP }
			}
			create_wargoal = {
				type = annex_everything
				target = JAP
			}
		}
	}
}
